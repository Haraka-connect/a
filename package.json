{
  "name": "haraka-connect-backend",
  "version": "1.0.0",
  "description": "Backend for Haraka Connect Ltd.",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Building backend...' && tsc",  // Replace with build commands if needed
    "start": "node index.js",  // Starts your Node.js server
    "dev": "nodemon index.js", // Development server
    "test": "jest"  // Add testing framework if needed
  },
  "dependencies": {
    "express": "^4.18.2",  // For creating APIs
    "mongoose": "^7.0.0",  // MongoDB ODM
    "jsonwebtoken": "^9.0.0",  // For user authentication
    "dotenv": "^16.0.0"  // Environment variable management
  },
  "devDependencies": {
    "nodemon": "^2.0.0",  // Hot reloading during development
    "jest": "^29.0.0",  // Testing framework (optional)
    "tsc": "^4.0.0"  // For TypeScript support if you use TypeScript
  },
  "engines": {
    "node": ">=18.x"
  },
  "type": "module"
}